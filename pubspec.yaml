name: eitherx
description: Either provides a way to handle errors and exceptions in a more expressive and concise way

version: 1.0.1
repository: https://github.com/hasanmhallak/Either

environment:
  sdk: ">=2.19.2 <3.0.0"

dependencies:
  meta: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
